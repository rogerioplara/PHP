<?php

    // BOOLEANS
    // são variaveis com 2 valores possíveis: true ou false

    $apresentar_nome = true;
    $apresentar_idade = false;

    // case insensitive
    $mostrar = 10;

    /**
     * variáveis usadas maioritariamente em estruturas de controle e fluxo de código
     * 
     * ciclos (loops) e instruções condicionais
     * 
     * o valor zer (0) é equivalente a false e todos os outros valores são true
     * 
     * podemos ainda determinar se uma variável é booleana ou não
     */

     var_dump(is_bool($mostrar));